# Example environment variables for Coolify
#
# These are not strictly required by the stack, but they provide a
# conventional place to record values you may also reference in docs
# or in templated configs.
#
# In Coolify, add these as environment variables on the app if you
# want to refer to them from commands or documentation.

# Public URL of your headscale instance as seen by clients.
HEADSCALE_PUBLIC_URL=https://headscale.example.com

# Base domain used for headscale MagicDNS.
HEADSCALE_TAILNET_DOMAIN=tailnet.example.internal
