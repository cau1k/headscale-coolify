# Example environment file for local development
#
# This file is optional and primarily useful if you want to run
# `docker-compose up` locally instead of through Coolify.
#
# The docker-compose.yml in this repo sets sensible defaults for most
# variables, but you can override them here for local testing.

# Base domain used for headscale MagicDNS.
HEADSCALE_DNS_BASE_DOMAIN=tailnet.example.internal

# IP prefixes for your tailnet.
HEADSCALE_IPV4_PREFIX=100.64.0.0/10
HEADSCALE_IPV6_PREFIX=fd7a:115c:a1e0::/48

# Enable MagicDNS.
HEADSCALE_MAGIC_DNS=true

# Logging level.
HEADSCALE_LOG_LEVEL=info
