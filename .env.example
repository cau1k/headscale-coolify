# Example environment file for local development
#
# This file is optional and primarily useful if you want to run
# `docker-compose up` locally instead of through Coolify.
#
# The docker-compose.yml in this repo does not strictly depend on these
# variables, but you may choose to reference them in your own overrides.

HEADSCALE_PUBLIC_URL=https://headscale.example.com
HEADSCALE_TAILNET_DOMAIN=tailnet.example.internal
